<%- include('partials/_header') %>

<div class="col-6 offset-3">
    <div class="row">
        <div class="card bg-secondary">
            <div class="card-body">
                <form action="/editarNota/<%= nota._id %>" method="POST">
                    <div class="form-group my-2">
                        <input type="text" name="titulo" value="<%= nota.titulo %>" class="form-control bg-dark text-light">
                    </div>
                    <div class="form-group my-4">
                        <textarea name="descripcion" rows="10" class="form-control bg-dark text-light"><%= nota.descripcion %></textarea>
                    </div>
                    <button type="submit" class="btn w-100 btn-primary">Editar</button>
                </form>
            </div>
        </div>
    </div>
</div>

<%- include('partials/_footer') %>